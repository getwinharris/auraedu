# AuraEdu repository map

> This map is generated by `php cli/generate-code-map.php`. Run the generator after adding, removing, or moving repository files.

```mermaid
flowchart LR
  root["AuraEdu repository"]
  app["app — controllers, services, routing"]
  views["views — customer and admin templates"]
  content["content — blog and help Markdown"]
  storage["storage/schema — persisted collection definitions"]
  config["config — non-secret runtime configuration"]
  integrations["integrations — external clients"]
  cli["cli — repeatable project commands"]
  tests["tests — application contracts"]
  docs["docs — durable product and hosting guidance"]
  github[".github — pull-request CI"]
  agents[".agents — support and admin-blog role prompts"]
  root --> app & views & content & storage & config & integrations & cli & tests & docs & github & agents
```

Run the generator to refresh the complete file list.
