---
name: bapxphp-cli
description: Use when running or changing the repository-owned bapXaura CLI, including orientation, validation, maps, knowledge indexes, schema, database, logs, blogs, images, and development-user commands.
---

# bapXaura CLI

Treat `bapXaura help` and the dispatch in `cli/bapXaura` as executable evidence.
Do not infer a command from old documentation. Normal coding tools and plain Git
remain valid; the CLI is for project-owned workflows, not a wrapper requirement for
every filesystem or Git operation.

## Orientation and query workflow

```bash
bapXaura help
bapXaura map
bapXaura schema list
bapXaura route:list
bapXaura skills
bapXaura status
bapXaura understand
```

Read `index.yaml` discovery and query it narrowly. Use `docs/project-index.json` for
existence, `map.mmd` for wiring, original files for truth, and `/remotedb` for live
admin-editable values.

## Validation and generated artifacts

```bash
bapXaura lint path/to/changed.php
bapXaura test
bapXaura update
bapXaura ci
bapXaura codemap
```

`update` regenerates committed maps and indexes; `ci` checks drift. Never hand-edit
generated maps or `index.yaml`. There is no `bapXaura index` or `bapXaura docsmap`
command; the knowledge index and content map are regenerated by `update`.

## Content, database and operations

```bash
bapXaura read blog [slug]
bapXaura write blog [slug]
bapXaura db query <collection> [filters]
bapXaura db find <collection> <id>
bapXaura db init
bapXaura db status
bapXaura logs [--limit N]
```

Blog commands operate on Markdown files. Product and other admin-editable values
must come from hosted MySQL. `db sync` currently performs initialization only; do
not describe it as JSON/YAML import or export. There is no `bapXaura blog:image`
or `bapXaura dev:user` command — upload media through Admin → Media.

## Scope

There is no `bapXaura tui` or application-agent CLI. The two application agents are
the customer support and owner/admin web surfaces. User attachments and web QA use
the active coding client's explicit tools and paths, outside this application CLI.

## Testing

For a CLI change, compare `bapXaura help` with the dispatch and target file, execute
the smallest safe command, then run `bapXaura ci`. Never print environment files,
credentials, secret values or customer records.

## Keeping this skill current

Update this skill when help, dispatch, an implementation file, or the binding
contract changes. A help entry without an implementation is a gap, not a feature.
